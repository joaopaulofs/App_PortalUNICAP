<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.unicap.navigationdrawer" >
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".post.DetailsActivityPost"></activity>
        <activity android:name=".disciplina.DetailsActivityDisciplina"></activity>
        <activity android:name=".professor.DetailsActivityProfessor"></activity>
        <activity android:name=".evento.DetailsActivityEvento"></activity>
    </application>

</manifest>
